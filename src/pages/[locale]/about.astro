---
import { prefixedLocales } from "@/core/i18n";
import AboutPage from "@/features/about/ui/pages/AboutPage.astro";

export function getStaticPaths() {
  return prefixedLocales.map((locale) => ({ params: { locale } }));
}
---

<AboutPage />
