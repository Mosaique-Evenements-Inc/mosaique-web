---
import { getQuoteTranslations } from "../../i18n";
import { getLocaleFromUrl } from "@/core/i18n";

const quoteText = getQuoteTranslations(getLocaleFromUrl(Astro.url));
---

<figure class="contact-media" data-media-status="pending" aria-hidden="true">
  <div class="contact-media__placeholder">
    <span>MOSAÏQUE EVENTS</span>
    <small>{quoteText.pendingMedia}</small>
  </div>
</figure>
